# SuperMate-LLM 发布清单（GitHub Release）

> **发布物共 7 项**：1 个 wheel + 1 个节点包 + 3 个工作流 + 2 份文档。
> 整合包整体不在 GitHub 发布（走 Bilibili 学习自用渠道）。

## 一、发布内容

| 文件 | 大小 | 说明 |
|---|---|---|
| `llama_cpp_python-0.3.35-py3-none-win_amd64.whl` | 631 MB | 便携版 llama-cpp-python：让 ComfyUI 能跑 Qwen3.5~3.8 模型；RTX 20系~50系可用；捆绑 CUDA 13，免装 CUDA |
| `ComfyUI_SuperMate-LLM-Nodes.zip` | 10.3 KB | **6 个** SuperMate LLM 节点（MiniMax 提示词增强 / **多图版** / 图片反推 / Text Gen / Loader / Unload） |
| `workflows/SuperMate-MiniMax提示词增强(UI).json` | 6.2 KB | 一张图 / 一句话 → 完整 MiniMax H3 提示词（图生/文生/续段，含用法说明） |
| `workflows/SuperMate-多图提示词增强(UI).json` | 10.6 KB | **多图版**：最多 5 张参考图 + 视频抽帧 → 多图综合 H3 提示词（4 个 LoadImage 入口） |
| `workflows/SuperMate-图片反推(UI).json` | 5.4 KB | 图片 → 描述提示词（约 35 秒，含画面比例，含用法说明） |
| `README.md` | 3.9 KB | 仓库首页说明 |
| `安装说明.md` | 6.4 KB | 完整参数文档（随 Bilibili 整合包分发） |

wheel 与节点包**必装**；工作流可选（放 `ComfyUI/user/default/workflows/` 或 Workflow → Open 打开）；后两份为文档。

## 二、安装（三步）

### 第一步 · 安装便携版 llama-cpp-python

先**关闭 ComfyUI**，再执行：

```powershell
pip install llama_cpp_python-0.3.35-py3-none-win_amd64.whl
```

目的：让 llama.cpp 在 ComfyUI 中运行 **Qwen3.5 ~ 3.8** 系列模型。
便携指令集编译（无 AVX-512 要求），**RTX 20系 ~ 50系显卡均可正常工作**；已捆绑 CUDA 13 运行时，**无需预装 CUDA**。

### 第二步 · 安装节点包

解压 `ComfyUI_SuperMate-LLM-Nodes.zip` 到 `ComfyUI/custom_nodes/`，得到：

```
ComfyUI/custom_nodes/ComfyUI_SuperMate-LLM-Nodes/
```

内含 **5 个节点**，让 Qwen3.8 系列 GGUF（**带视觉 mmproj**）在 ComfyUI 中工作：支持**图片反推**、**MiniMax H3 提示词强化**。**无需安装其他依赖**。

### 第三步 · 放模型

GGUF 主模型（如 `Qwen3.8-27B-UD-IQ3_XXS.gguf`）与视觉模型（`Qwen3.8-27B-mmproj-BF16.gguf`）放入 **`ComfyUI/models/LLM/`**，重启 ComfyUI 即可使用。

> 让 Qwen3.8 视觉文本模型带我们起飞吧！

## 三、安装后目录结构

```
ComfyUI/
├── custom_nodes/
│   └── ComfyUI_SuperMate-LLM-Nodes/
│       ├── __init__.py
│       └── nodes.py
├── models/
│   └── LLM/
│       ├── Qwen3.8-27B-UD-IQ3_XXS.gguf      （主模型）
│       └── Qwen3.8-27B-mmproj-BF16.gguf     （视觉投影，看图/反推必选）
└── user/default/workflows/
    └── SuperMate/                           （2 个工作流，可选放置）
```

## 四、需要 API 工作流？

发布物**不含 API 版**——ComfyUI 中打开任一工作流，菜单「导出 (API)」即可一键生成。
本地自动化自用的 API 版在 `E:\Comfyui-SuperMate\api-workflows\`（不随 GitHub 发布）。

## 五、交付验证（已通过）

- [x] wheel 校验通过；DLL 已捆绑进 `llama_cpp/lib/`（含 CUDA13 运行时）
- [x] 节点包 import 通过（5 节点）；端口一一对应（模型↔模型 / IMAGE↔图片 / text↔text）
- [x] **Unload 节点修复（2026-08-28）**：新增通配输入+透传输出+OUTPUT_NODE，可串进工作流链尾自动卸载，也可独立放置随工作流执行（旧版无输入端口无法连线）——16 项单测全部 PASS
- [x] 2 个工作流实测跑通：MiniMax 提示词增强（图生+场景参考 / 图生+人物参考 / 文生 / 续段 四场景）、图片反推（582 字），均无思考痕迹、含分镜/转场/比例/时长
- [x] 单元测试全部 PASS（含 `<think>` 剥离用例与签名一致性检查）

## 六、GitHub 发布操作

1. 建 Release：Tag `v0.3.35`，标题 `SuperMate-LLM v0.3.35（wheel + 节点包）`；
2. 上传：wheel、节点包 zip、`workflows/` 下 2 个 json、`README.md`；
3. `安装说明.md` 随 Bilibili 学习自用整合包分发，不上传 GitHub。

> **版本号说明（2026-08-28 决定）**：`v0.3.35` 是沿用 llama-cpp-python 轮子的版本号（首个发布物未单独立项目版本号）。
> **后续项目更新（工作流/节点包改动）→ 改用 SuperMate-LLM 独立版本号**（v1.0.0 起，修复=v1.0.x，加功能=v1.x.0），
> 与 llama-cpp-python 解耦；wheel 的 0.3.35 只在 README 特性表里作为依赖版本注明。

## 七、发布规范（2026-08-28 用户认可，后续发布照此执行）

### README 主页格式（模板：`release/README-发布模板.md`）

1. **标题 + 一句话卖点标语**（引用块）；
2. **📥 下载（GitHub Release）区放第一屏**：资产表（文件/说明/直链）+ Release 总览页链接
   ——大文件（>100MB）只能走 Release 资产、git 仓库不显示，必须给直链；
3. 解决了什么问题（痛点 + 报错 + 解法）；
4. 特性表（版本/平台/硬件覆盖/运行时/体积）；
5. 安装（命令 + 组件表）；
6. 附带工作流表；
7. 复现参数（可选）→ 实测 → 许可证与致谢。

### GitHub 平台规则（踩坑沉淀）

- **资产名仅支持 ASCII**：中文/括号 `()` 会被剥离（`xxx提示词增强(UI).json` → `xxx.UI.json`），
  且同名规范化冲突报 422 already_exists → 资产名一律用 ASCII 语义名，中文对应写进 README；
- **>100MB 文件不能 git push**（限 100MB），必须走 Release 上传 API（uploads.github.com）；
- PowerShell `Invoke-WebRequest` 发 JSON 默认 GBK 中文变 `?` → 用 Python urllib（UTF-8）或 UTF-8 文件；
- 工具：`E:\Harness Workspace\comfyui_node_tests\gh_upload_asset.py`（资产上传）、`gh_patch_release.py`（name/body 修复）。
